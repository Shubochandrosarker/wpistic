name: wpistic-core tests

on:
  push:
    paths:
      - 'wpistic-core/**'
      - '.github/workflows/wpistic-core-tests.yml'
  pull_request:
    paths:
      - 'wpistic-core/**'
      - '.github/workflows/wpistic-core-tests.yml'

jobs:
  phpunit:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: wpistic-core
    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          coverage: none

      - name: Install Composer dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Run PHPUnit
        run: vendor/bin/phpunit
